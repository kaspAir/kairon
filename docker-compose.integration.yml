services:
  kairon-db:
    image: postgres:16
    container_name: kairon-db-int
    environment:
      POSTGRES_DB: kairon
      POSTGRES_USER: kairon
      POSTGRES_PASSWORD: kairon_int_password
    ports:
      - "5433:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U kairon -d kairon"]
      interval: 5s
      timeout: 5s
      retries: 10
    volumes:
      - kairon_db_data:/var/lib/postgresql/data

  kairon-app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: kairon-app-int
    environment:
      FLASK_ENV: integration
      DATABASE_URL: postgresql://kairon:kairon_int_password@kairon-db:5432/kairon
      LOG_LEVEL: INFO
    ports:
      - "5000:5000"
    depends_on:
      kairon-db:
        condition: service_healthy

  jenkins:
    build:
      context: .
      dockerfile: Dockerfile.jenkins
    container_name: kairon-jenkins
    user: root
    ports:
      - "8082:8080"
      - "50000:50000"
    volumes:
      - jenkins_home:/var/jenkins_home
      - /var/run/docker.sock:/var/run/docker.sock

volumes:
  kairon_db_data:
  jenkins_home:
